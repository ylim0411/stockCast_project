<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Store">
    <select id="selectAll" resultType="Store">
        SELECT
        *
        FROM store
        WHERE adminId = #{adminId}
        ORDER BY storeId ASC
    </select>
    <update id="update" parameterType="Store">
        UPDATE store
        SET
        storeName = #{storeName},
        storeAddress = #{storeAddress},
        storePhone = #{storePhone},
        storeEmail = #{storeEmail}
        WHERE storeId = #{storeId} AND adminId = #{adminId}
    </update>
    <insert id="insert" parameterType="Store">
        INSERT INTO store (
        adminId,
        storeName,
        storeAddress,
        storePhone,
        storeEmail
        ) VALUES (
        #{adminId},
        #{storeName},
        #{storeAddress},
        #{storePhone},
        #{storeEmail}
        )
    </insert>

    <select id="countByAdminId" resultType="int">
        SELECT COUNT(*) FROM store WHERE adminId = #{adminId}
        AND (
        storeName LIKE CONCAT('%', #{searchKeyword}, '%')
        OR storeAddress LIKE CONCAT('%', #{searchKeyword}, '%')
        )
    </select>

    <!-- 페이징된 점포 목록 -->
    <select id="selectPageByAdminId" resultType="Store">
        SELECT * FROM store
        WHERE adminId = #{adminId}
        AND (
        storeName LIKE CONCAT('%', #{searchKeyword}, '%')
        OR storeAddress LIKE CONCAT('%', #{searchKeyword}, '%')
        )
        ORDER BY storeId ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="isUnique" resultType="Store">
        SELECT * FROM store
        WHERE storeName = #{storeName}
    </select>
</mapper>