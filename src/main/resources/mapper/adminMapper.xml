<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Admin">
    <insert id="join" parameterType="Admin">
        INSERT INTO admin (adminName, loginId, loginPw, businessNumber)
        VALUES (#{adminName}, #{loginId}, #{loginPw}, #{businessNumber});
    </insert>
    <select id="login" resultType="Admin">
        SELECT * FROM admin WHERE loginId = #{loginId} AND loginPw = #{loginPw}
    </select>

    <select id="isJoinedById" resultType="Admin">
        SELECT * FROM admin WHERE loginId = #{email}
    </select>

    <delete id="deleteAdminById" parameterType="int">
        DELETE FROM admin WHERE adminId = #{adminId}
    </delete>

    <select id="checkId" resultType="String">
        SELECT loginId
        FROM admin
        WHERE loginId = #{loginId}
    </select>
</mapper>